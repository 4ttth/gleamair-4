{
  "name": "gleamair-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Gleamair Enterprises website and customer/staff portal",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vercel dev",
    "build:psgc": "python3 tools/build-psgc.py",
    "test": "node --import ./tests/loader.mjs tests/auth.test.mjs && node --import ./tests/loader.mjs tests/bookings.test.mjs"
  },
  "dependencies": {
    "mongodb": "^6.12.0"
  }
}
